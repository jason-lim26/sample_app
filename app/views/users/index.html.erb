<% provide(:title, 'All users') %>
<h1>All users</h1>

<%= will_paginate %>

<ul class="users">
  <% @users.each do |user| %>
    <%= render @users %>
  <% end %>
</ul>

<!--The 'paginate' method takes a hash argument with key :page and value to the
    page requested in the console -->
<%= will_paginate %>